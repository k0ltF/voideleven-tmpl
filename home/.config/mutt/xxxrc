# vim: filetype=muttrc

set from 	= "mail.xxx@custom-gmail.fr"
set sendmail 	= "/usr/bin/msmtp -a xxx"
set mbox	= "+xxx/INBOX"
set postponed	= "+[Gmail].Brouillons"
unset record
color status green default

macro index ,d "<change-folder>+yyy/INBOX<return>" "change to yyy email"
